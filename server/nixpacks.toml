[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "npm start"

[variables]
PORT = "8080"
NODE_ENV = "production"

[persistence]
paths = ["database.sqlite"] 